import 'package:flutter/material.dart';

class Test5 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('기능'),
      ),
      body: Center(child: Text('기능들', style: TextStyle(fontSize: 60),),),
    );
  }
}
