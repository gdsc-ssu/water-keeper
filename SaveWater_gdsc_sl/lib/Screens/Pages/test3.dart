import 'package:flutter/material.dart';

class Test3 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Shop'),
      ),
      body: Center(child: Text('설정', style: TextStyle(fontSize: 60),),),
    );
  }
}
